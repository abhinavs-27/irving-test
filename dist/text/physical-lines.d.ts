/**
 * Iterate physical lines with the character index where each line begins in `text`.
 */
export declare function eachPhysicalLine(text: string): Generator<{
    lineStart: number;
    line: string;
}>;
//# sourceMappingURL=physical-lines.d.ts.map